import React from "react";
import {
    Paper,
    Button,
    TextField,
    Typography,
    FormControlLabel,
    Checkbox,
} from "@material-ui/core";

export function CustomizeGroupView(props) {
    return (
        <div>CustomizeGroupView</div>
    );
}